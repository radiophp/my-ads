# Backend base URL (including /api)
BASE_URL=https://dev.mahanfile.com/api

# Authorization token (if required by backend). Leave blank if not needed.
# TOKEN=

# Path to the headers file containing full Divar request headers (one per line)
HEADERS_FILE=jwt.txt

# Seconds to sleep between iterations (rate-limit safety)
SLEEP=10

# Playwright defaults (force Firefox UI; adjust profile if you want your saved session)
PW_BROWSER=firefox
# Leave blank to use Playwright’s bundled Firefox (recommended). Set only if you really need a specific executable.
FIREFOX_EXECUTABLE=
# If you want to reuse an existing Firefox profile, set the path below. Otherwise a fresh profile is used.
FIREFOX_USER_DIR=

# Worker identifier (unique per process)
# WORKER_ID=worker-1
